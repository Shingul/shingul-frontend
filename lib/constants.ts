/**
 * Application constants
 */

/**
 * Maximum word limit for study set descriptions
 */
export const STUDY_SET_DESCRIPTION_MAX_WORDS = 10000;

/**
 * Maximum number of PDF files that can be uploaded at once
 */
export const MAX_PDF_FILES = 3;

/**
 * Maximum number of flashcards that can be generated at once
 */
export const MAX_FLASHCARD_LIMIT = 100;

/**
 * Maximum number of quiz questions that can be generated at once
 */
export const MAX_QUIZ_QUESTIONS_LIMIT = 100;

/**
 * Maximum number of players that can join a game
 */
export const MAX_PLAYERS_LIMIT = 7;

/**
 * Maximum character length for study set titles
 */
export const MAX_TITLE_LENGTH = 100;
